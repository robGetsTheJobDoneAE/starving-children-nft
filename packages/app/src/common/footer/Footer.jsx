import Nullstack, { NullstackClientContext } from "nullstack";

class Footer extends Nullstack {
  //   prepare({ page }: NullstackClientContext) {
  //     page.locale = 'en-US';
  //   }

  render() {
    return <div>Hello I am footer</div>;
  }
}

export default Footer;
